# lib/business/persona.mjs — 评分模式参考

> 本文件是 [persona.md](./persona.md) §3.3-3.4 的展开。完整列举 `scoreMessage(text)` 的所有打分模式。

## 1. 文本打分 `scoreMessage(text)`

```js
export function scoreMessage(text, opts = {}) {
  // 招呼强信号先判 (避免长度盖掉)
  // 长度加权
  // 俚语命中
  // formal 关键词命中
  // emoji / 问号 / 礼貌语
  // → 返回 {mode: 'human'|'formal', score, reason}
}
```

## 2. 招呼强信号 (优先判定)

```js
const GREETING_FORMAL = /^\s*(在吗|你好|您好|hi|hello|hey|早上好|晚上好|下午好)[?？!！。.\s]*$/i;
const GREETING_HUMAN  = /^\s*(咋了|咋啦|干嘛呢|干啥|搞啥|在么|在?[哈嘿]?|咋回事)\b/;
```

| 模式 | 加分 | 备注 |
|---|---|---|
| `在吗 / 你好 / 您好 / hi / hello / hey` | formal +0.85 | 礼貌招呼 |
| `咋了 / 咋啦 / 干嘛呢 / 干啥 / 在么` | human +0.85 | 口语招呼 |

注意: 中文没有 word boundary, `在?` 不会匹配 "在乎" / "在意" 等无关词, 因为 `\b` 在中文里通常不起作用, 所以这里用 `^\s*...` + 空白/结尾控制。

## 3. 长度加权

```js
const len = [...t].length; // 中文按字计数
```

| 长度 | 加分 |
|---|---|
| 空消息 (`@大黄鱼` 无内容) | human +0.20 |
| ≤6 字 | human +0.55 |
| 7-15 字 | human +0.20 |
| 16-40 字 | formal +0.10 |
| 41-120 字 | formal +0.30 |
| >120 字 | formal +0.50 |

> "中文按字计数" `[...t].length` 处理 emoji 和中文标点更准确 (避免 utf-16 surrogate 拆分)。

## 4. 俚语命中 (human +0.40)

```js
const SLANG_PATTERNS = [
  // 语气词 + 抽象梗
  /笑死/, /服了/, /好家伙/, /我超/, /\b牛马\b/, /牛马(?!屎)/, /牛来/, /搁这/,
  /儒雅随和/, /寄吧/, /勾八/, /小卡拉米/, /孙笑川/, /\bNB\b/i, /\b666\b/, /\byyds\b/,
  /\bemo\b/i, /破防/, /压力AI/, /压力 ai/i,
  // 高频口语
  /真有你的/, /嗯嗯/, /\b啧\b/, /\b艹\b/, /卧槽/, /怎么滴/, /\b滴\b/,
  /\b咋\b/, /\b嫩\b/, /不会吧/, /搁这装/, /纯纯/, /纯牛/, /搁这搁这/,
  /搁这/, /笑死我/, /离谱/, /受不了/, /无语\(?！/, /绝了/, /绷不住/,
  /哈哈哈/, /哈哈哈哈/, /傻卵/, /滚蛋/, /喷粪/, /真行/, /这波/,
  // 重复/刷屏型: !!+ / ??+
  /!{2,}/, /\?{2,}/,
];
```

命中任一 → human +0.40, 累积不重复 (每个 pattern 命中只算一次)。

## 5. 正式关键词命中 (formal +0.40)

```js
const FORMAL_PATTERNS = [
  // 礼貌请求
  /\b请[问求帮]\b/, /\b请问\b/, /\b麻烦\b/, /\b能不能\b/, /\b可以[吗帮]\b/,
  // 任务型动词开头
  /^(帮我|请帮我|麻烦你|能不能帮我|帮我[一-龥]*?[下吧呢啊]?)/,
  // 明确任务: 翻译/总结/解释/整理/分析/写/算/查
  /翻译/, /总结/, /解释/, /整理/, /分析/, /(写|改|做)一?[下份个段篇首首]/, /算一?下/, /查一?下/,
  // 文件/代码/技术相关
  /\b代码\b/, /\bbug\b/i, /\b报错\b/, /\b函数\b/, /\b正则\b/, /\bapi\b/i,
  /\bsql\b/i, /\bgit\b/i, /\bdocker\b/i, /\bjson\b/i, /\bcsv\b/i, /\byaml\b/i, /\bhttp\b/i,
  // 信息类问题模板
  /(怎么|如何|怎样|啥是|什么是|为啥|为什么|哪[儿里]有)/,
  // 数字/参数化
  /^\d+[\.、]/,  // 1./2./3. 列表开头
];
```

命中任一 → formal +0.40。

## 6. emoji / 问号 / 礼貌语 (辅助信号)

| 信号 | 加分 |
|---|---|
| emoji 数 >2 | human +0.20 |
| 多问号 `??+` | human +0.20 |
| 礼貌词 (谢谢 / 感谢 / 辛苦了) | formal +0.30 |

## 7. 累计计算

```js
const total = s.human - s.formal;
// 总分高的一方胜出; |total| < tieMargin → 退到 defaultMode
```

## 8. 实战例子

| 输入 | human | formal | 模式 | reason |
|---|---|---|---|---|
| `在吗` | 0 | 0.85 | GREETING_FORMAL | 礼貌招呼 |
| `咋了` | 0.85 | 0 | GREETING_HUMAN | 口语招呼 |
| `@大黄鱼` | 0.20 | 0 | len=0 | 空消息 |
| `你🐴呢` | 0.40 | 0 | 俚语"你🐴呢" | 俚语命中 |
| `帮我翻译这段` | 0 | 0.80 | "帮我" + "翻译" | 任务型 |
| `今天金价多少` | 0 | 0.50 | len=6 + 问号 | 中等长度 |
| `哈哈哈笑死` | 0.85 | 0 | 多个俚语 | 俚语 + emoji |

## 9. 测试时手动注入

```js
import { scoreMessage } from './persona.mjs';
scoreMessage('在吗', { now: new Date('2026-09-07T03:00:00+08:00') });
// → {mode: 'formal', score: {...}, reason: '礼貌招呼 — 强信号'}

scoreMessage('咋了', { hourBias: 3 });
// → {mode: 'human', score: {human: 1.0, formal: 0, signals: [...]}, reason: '口语招呼 — 强信号'}
```